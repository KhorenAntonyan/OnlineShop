<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <environment include="Development">
        <link rel="stylesheet" href="~/bootstrap/css/bootstrap.css" />
        <link rel="stylesheet" href="~/css/site.css" />
    </environment>
    <environment exclude="Development">
        <link rel="stylesheet" href="~/bootstrap/css/bootstrap.min.css" />
        <link rel="stylesheet" href="~/css/site.min.css" />
    </environment>
</head>
<body>
    @*<header>*@
    @*    <nav class="navbar navbar-expand-lg navbar-light bs-side-navbar" style="background-color:turquoise;">
        <div class="container-fluid">
        <a class="navbar-brand" asp-controller="Home" asp-action="Index">Online Shop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav mynavbar-nav mr-auto">
        <li class="nav-item">
        <a class="nav-link" aria-current="page" asp-controller="Product" asp-action="Product">Product</a>
        </li>
        <li class="nav-item">
        <a class="nav-link" aria-current="page" asp-controller="Category" asp-action="Category">Category</a>
        </li>


        </ul>
        @if (User.Identity.IsAuthenticated)
        {
        <p>@User.Identity.Name</p>

        <form method="post" asp-controller="Account" asp-action="Logout">
        <input type="submit" value="Выход" />
        </form>
        }
        else
        {
        <a asp-controller="Account" asp-action="Login">Вход</a>
        <a asp-controller="Account" asp-action="Register">Регистрация</a>
        };
        </div>
        </div>
        </nav>*@

    <div class="sidebar">
        <h2><a class="navbar-brand" asp-controller="Home" asp-action="Index">Online Shop</a></h2>
        <ul>
            <li>
                <a asp-controller="Product" asp-action="Product"><i class="fas fa-solid fa-clipboard-list"></i>Product</a>
            </li>
            <li>
                <a asp-controller="Category" asp-action="Category"><i class="fas fa-solid fa-list"></i>Category</a>
            </li>
        </ul>
        <div class="user-bar">
            @if (User.Identity.IsAuthenticated)
            {
                <p>@User.Identity.Name</p>

                <form method="post" asp-controller="Account" asp-action="Logout">
                    <input type="submit" value="Выход" />
                </form>
            }
            else
            {
                <a asp-controller="Account" asp-action="Login"><i class="fas fa-user"></i>Login</a>
                <a asp-controller="Account" asp-action="Register">Register</a>
            };
        </div>
    </div>
    @*</header>*@
    <div class="container main_content">
        @if (TempData["message"] != null)
        {
            <div class="alert alert-success">@TempData ["message"]</div>
        }
        @RenderBody()
    </div>




    <script src="~/bootstrap/js/bootstrap.js"></script>
    <script src="https://kit.fontawesome.com/b99e675b6e.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
